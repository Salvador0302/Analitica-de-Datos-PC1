# Cache de bytecode de Python (no necesario versionar)
__pycache__/
# Archivos compilados .pyc/.pyo/.pyd generados automáticamente
*.py[cod]

# Archivo local de variables de entorno (puede contener credenciales)
.env
# Carpeta de entorno virtual (variante .venv)
.venv/
# Carpeta de entorno virtual (nombre común venv)
venv/

# Carpeta de logs generados por la aplicación (no relevante para el repo)
logs/

# Checkpoints automáticos de Jupyter (ruido)
.ipynb_checkpoints/

# Datasets CSV grandes (evitar subir ~GB de datos)
data/**/*.csv
# Datasets Parquet grandes (formato columnar)
data/**/*.parquet

# Configuración y estado local de Streamlit (regenerable)
.streamlit/

# Archivos de metadatos
.DS_Store
Thumbs.db
